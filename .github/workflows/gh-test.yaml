---
name: Test

on: [push]

jobs:
  test:
    name: Test
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v1
    - name: Run Go Test
      id: gotest
      uses: docker://golang:1.13
      with:
        args: go test */***